extends layout

include mixins/_cards

block styles
  link( rel="stylesheet", href="css/app.bundle.css" )

block content
  each card in cards
    .card( id=card._id )  
      .front.inner
        +frontCard( card )
      .back.inner
        +backCard( card )

block scripts
  script( src="js/app.bundle.js" )
